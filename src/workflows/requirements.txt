# マーケティング企画・制作エージェント - 依存関係

# HTTP リクエスト（SerpAPI連携用）
requests==2.31.0

# 環境変数管理
python-dotenv==1.0.0

# JSON処理・データ操作（標準ライブラリで十分なため、追加不要）

# 進捗確認・ファイル操作（標準ライブラリ使用）
# pathlib, json, re, datetime は標準ライブラリ

# 必要に応じて追加可能なオプションパッケージ：
# beautifulsoup4==4.12.2  # HTMLパース（LP分析用）
# selenium==4.15.0        # ブラウザ自動化（E2Eテスト用）
# pillow==10.0.1          # 画像処理（LP画像最適化用） 